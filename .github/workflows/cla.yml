name: "CLA Assistant"
on:
  # TEMPORARY: Use 'pull_request' so it runs the code in THIS branch
  pull_request:
    types: [opened, synchronize, closed]

jobs:
  CLAAssistant:
    runs-on: ubuntu-latest
    steps:
      - name: "CLA Assistant"
        if: (github.event.comment.body == 'recheck' || github.event.comment.body == 'I have read the CLA Document and I hereby sign the CLA') || github.event_name == 'pull_request'
        # UPGRADE: Changed from v2.3.0 to v2.6.1 to fix the token bug
        uses: contributor-assistant/github-action@v2.6.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PERSONAL_ACCESS_TOKEN: ${{ secrets.CLA_PERSONAL_ACCESS_TOKEN }}
        with:
          path-to-signatures: 'signatures/version1/cla.json'
          path-to-document: 'https://github.com/forem/forem/blob/main/CLA.md'
          branch: 'main'
          allowlist: ${{ secrets.CLA_ALLOWLIST }}
          # HARDCODED FIX: Keeping this to ensure no secret typos
          remote-repository-name: 'forem/CLA-signatures'
          lock-pullrequest-aftermerge: false