apiVersion: v1
kind: Service
metadata:
  name: noema-web
  namespace: noema
  labels:
    app.kubernetes.io/name: noema
    app.kubernetes.io/component: web
    app.kubernetes.io/part-of: noema
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: noema
    app.kubernetes.io/component: web
  ports:
    - name: http
      port: 80
      targetPort: http
