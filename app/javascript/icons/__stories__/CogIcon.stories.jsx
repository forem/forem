import { h } from 'preact';
import { CogIcon } from '../CogIcon';

export default {
  title: 'Components/Icons',
};

export const Default = () => <CogIcon />;

Default.story = {
  name: 'cog icon',
};
