/* global addLocalizedDateTimeToElementsTitles */
function initializeCreditsPage() {
  // const dates = document.querySelectorAll('.ledger time');
  // const replaceText = (element, localizedDateTime) => {
  //   element.textContent = localizedDateTime;
  // }
  // addLocalizedDateTimeToElementsTitles(dates, 'datetime', { callback: replaceText });
}
