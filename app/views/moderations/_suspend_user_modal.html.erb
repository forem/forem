<div id="suspend-user-modal">
  <p>Once suspended, <%= @moderatable.username %> will not be able to comment or publish posts until their suspension is removed.</p>

  <div class="crayons-field mt-4">
    <label for="suspend_reason_userid_<%= @moderatable.user.id %>" class="crayons-field__label">Note:</label>
    <textarea id="suspend_reason_userid_<%= @moderatable.user.id %>" rows="4" cols="70" placeholder="Reason for suspension" class="crayons-textfield" required></textarea>
  </div>
  <div class="mt-4">
    <button id="submit-user-suspension-btn" class="c-btn c-btn--destructive c-btn--primary">Submit & Suspend</button>
  </div>
</div>
