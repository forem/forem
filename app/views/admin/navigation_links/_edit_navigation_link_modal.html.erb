<span data-controller="modal" data-modal-root-id-value="edit-navigation-link-<%= link[:id] %>-modal-root" data-modal-content-id-value="edit-navigation-link-<%= link[:id] %>-modal" data-modal-title-value="Edit Link" data-modal-size-value="l">
  <button data-action="modal#toggleModal" class="color-accent-warning-darker crayons-btn crayons-btn--ghost-warning">
    Edit Link
  </button>

  <div id="edit-navigation-link-<%= link[:id] %>-modal" class="hidden">
    <div class="form-group grid p-6 mb-6 gap-1">
      <%= form_for [:admin, link] do |form| %>
        <%= render "form", form: form %>
        <%= form.submit class: "btn btn-primary" %>
      <% end %>
    </div>
  </div>

   <div id="edit-navigation-link-<%= link[:id] %>-modal-root"></div>
</span>
