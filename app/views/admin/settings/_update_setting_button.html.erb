<% if current_user.has_role?(:super_admin) %>
  <%= f.submit "Update Settings", class: "crayons-btn mt-4", aria: { label: defined?(aria_label) ? aria_label : nil }, data: { disable_with: false } %>
<% end %>
