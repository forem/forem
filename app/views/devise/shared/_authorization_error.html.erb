<% unless flash[:alert].blank? %>
  <div class="crayons-notice--danger registration__error-notice">
    <% if flash[:alert].is_a?(Array) %>
      Something went wrong:
      <ul>
        <% flash[:alert].each do |error| %>
          <li>
            <%= error %>
          </li>
        <% end %>
      </ul>
    <% else %>
      <strong>
        <%= flash[:alert] %>
      </strong>
      <br>
    <% end %>
    Якщо ви ще не отримали свій лист з підтвердженням пошти, то <a href="<%= confirm_email_path %>">натисніть сюди</a> щоб ми надіслали його знову.
    <br>
    <%= t("contact_prompts.if_continued_trouble_html") %>
  </div>
<% end %>
