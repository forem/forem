<div class="profile-preview-card crayons-dropdown" style="--card-color: <%= Color::CompareHex.new([user_colors(@user)[:bg], user_colors(@user)[:text]]).brightness(0.88) %>; border-top: var(--su-7) solid var(--card-color);">
    <div class="gap-4 grid">
        <%= render "shared/profile_card_content", context: "preview-card", actor: @user %>
    </div>
</div>
