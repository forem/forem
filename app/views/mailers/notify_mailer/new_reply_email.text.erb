<%= @comment.user.name %> відповіли вам на сайті <%= @comment.parent_type %>:

<%= ActionController::Base.helpers.strip_tags(@comment.processed_html.html_safe) %>

View now: <%= comment_url(@comment) %>
