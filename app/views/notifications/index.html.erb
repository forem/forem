<% title "Notifications" %>

<%= content_for :page_meta do %>
  <link rel="canonical" href="<%= app_url("/notifications") %>" />
  <meta name="description" content="Notifications inbox for dev.to">
  <meta name="keywords" content="software development,engineering,rails,javascript,ruby">

  <meta property="og:type" content="article" />
  <meta property="og:url" content="<%= app_url("/notifications") %>" />
  <meta property="og:title" content="Notifications - dev.to" />
  <meta property="og:description" content="Notifications inbox for dev.to" />
  <meta property="og:site_name" content="<%= community_qualified_name %>" />

  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:site" content="@<%= SiteConfig.social_media_handles["twitter"] %>">
  <meta name="twitter:title" content="Notifications - dev.to">
  <meta name="twitter:description" content="Notifications inbox for dev.to">
<% end %>

<% if user_signed_in? %>
  <div class="home" id="notifications-container">
    <div class="side-bar">
      <div class="crayons-card fixed-widget">
        <header class="crayons-card__header">
          <h3 class="crayons-card__title">Filter</h3>
        </header>
        <div class="crayons-card__body widget-body">
          <section class="crayons-card__inner-section">
            <% if @organizations.present? %>
              <header class="crayons-card__subheader">
                <h4 class="crayons-card__subheader__title">
                  <span>@<%= @user.username %></span>
                </h4>
              </header>
            <% end %>
            <fieldset class="crayons-fieldset">
              <a class="query-filter-button <%= "selected" if params[:filter].blank? %>" href="/notifications">
                <div class="crayons-field crayons-field--radio">
                  <input type="radio" name="all" id="r2" class="crayons-radio" <%= "checked" if params[:filter].blank? %>>
                  <label for="all" class="crayons-field__label">
                    All
                  </label>
                </div>
              </a>
              <a class="query-filter-button <%= "selected" if params[:filter].to_s.casecmp("comments").zero? && params[:org_id].blank? %>" href="/notifications/comments">
                <div class="crayons-field crayons-field--radio">
                  <input type="radio" name="name1" id="r2" class="crayons-radio" <%= "checked" if params[:filter].to_s.casecmp("comments").zero? && params[:org_id].blank? %>>
                  <label for="r2" class="crayons-field__label">
                    Comments
                  </label>
                </div>
              </a>
              <a class="query-filter-button <%= "selected" if params[:filter].to_s.casecmp("posts").zero? && params[:org_id].blank? %>" href="/notifications/posts">
                <div class="crayons-field crayons-field--radio">
                  <input type="radio" name="name1" id="r2" class="crayons-radio" <%= "checked" if params[:filter].to_s.casecmp("posts").zero? && params[:org_id].blank? %>>
                  <label for="r2" class="crayons-field__label">
                    Posts
                  </label>
                </div>
              </a>
            </fieldset>
          </section>
          <section class="crayons-card__inner-section">
            <% if @organizations.present? %>
              <% @organizations.each do |org| %>
                <header class="crayons-card__section-header">
                  <h4 class="crayons-card__secondaray-header__title">
                  <span>@<%= org.slug %></span>
                  </h4>
                </header>
                <fieldset class="crayons-fieldset">
                  <a class="query-filter-button <%= "selected" if params[:filter].to_s.casecmp("org").zero? && params[:org_id].to_i == org.id %>" href="/notifications/org/<%= org.id %>">
                    <div class="crayons-field crayons-field--radio">
                      <input type="radio" name="name1" id="r2" class="crayons-radio" <%= "checked" if params[:filter].to_s.casecmp("org").zero? && params[:org_id].to_i == org.id %>>
                      <label for="r2" class="crayons-field__label">
                        All
                      </label>
                    </div>
                  </a>
                  <a class="query-filter-button <%= "selected" if params[:filter].to_s.casecmp("comments").zero? && params[:org_id].to_i == org.id %>" href="/notifications/comments/<%= org.id %>">
                    <div class="crayons-field crayons-field--radio">
                      <input type="radio" name="name1" id="r2" class="crayons-radio" <%= "checked" if params[:filter].to_s.casecmp("comments").zero? && params[:org_id].to_i == org.id %>>
                      <label for="r2" class="crayons-field__label">
                        Comments
                      </label>
                    </div>
                  </a>
                </fieldset>
              <% end %>
            <% end %>
          </section>
        </div>
      </div>
    </div>
    <div class="articles-list" id="articles-list">
      <section class="notifications-filter-menu__mobile">
        <% if @organizations.present? %>
          <% @organizations.each do |org| %>
            <div class="crayons-dropdown__container" aria-label="notifications-filter-menu">
              <button type="button" id="notifications-filter__select" class="crayons-dropdown__select">
                <% if params[:org_id].blank? %>
                  @<%= @user.username %>
                <% else %>
                  @<%= org.slug %>
                <% end %>
                <%= inline_svg_tag("arrow-down-fill.svg", aria: true, class: "crayons-icon", title: "Arrow Down") %>
              </button>
            </div>
            <div class="crayons-dropdown__menu-overlay" id="notifications-filter__menu-overlay">
              <nav class="crayons-dropdown__menu notifications-filter__menu" aria-label="notifications-filter-menu">
                <header class="crayons-dropdown__header">
                  <h3 class="crayons-dropdown__title">
                    Filter
                  </h3>
                </header>
                <a href="/notifications" class="crayons-dropdown__option <%= "selected" if params[:org_id].blank? %>">
                  <div class="crayons-dropdown__label">
                    <span class="crayons-dropdown__label--title">
                      <%= @user.name %>
                    </span>
                    <span class="crayons-dropdown__label--subtitle">
                      @<%= @user.username %> · Personal
                    </span>
                  </div>
                  <div class="crayons-dropdown__icon">
                    <%= inline_svg_tag("checkmark.svg", aria: true, class: "crayons-icon", title: "Checkmark") %>
                  </div>
                </a>
                <a href="/notifications/org/<%= org.id %>" class="crayons-dropdown__option <%= "selected" if params[:org_id].to_i == org.id %>">
                  <div class="crayons-dropdown__label">
                    <span class="crayons-dropdown__label--title">
                      <%= org.name %>
                    </span>
                    <span class="crayons-dropdown__label--subtitle">
                      @<%= org.slug %> · Organization
                    </span>
                  </div>
                  <div class="crayons-dropdown__icon">
                    <%= inline_svg_tag("checkmark.svg", aria: true, class: "crayons-icon", title: "Checkmark") %>
                  </div>
                </a>
              </nav>
            </div>
          <% end %>
        <% end %>
        <div class="crayons-tabs notifications-mobile-filters <%= "showing" if params[:org_id].blank? %>">
          <a class="crayons-tabs--item <%= "crayons-tabs--item--current" if params[:filter].blank? %>" href="/notifications">
            <span class="crayons-tabs__label">
              All
            </span>
          </a>
          <a class="crayons-tabs--item <%= "crayons-tabs--item--current" if params[:filter].to_s.casecmp("comments").zero? && params[:org_id].blank? %>" href="/notifications/comments">
            <span class="crayons-tabs__label">
              Comments
            </span>
          </a>
          <a class="crayons-tabs--item <%= "crayons-tabs--item--current" if params[:filter].to_s.casecmp("posts").zero? && params[:org_id].blank? %>" href="/notifications/posts">
            <span class="crayons-tabs__label">
              Posts
            </span>
          </a>
        </div>
        <% if @organizations.present? %>
          <% @organizations.each do |org| %>
            <div class="crayons-tabs notifications-mobile-filters <%= "showing" if params[:org_id].to_i == org.id %>">
              <a class="crayons-tabs--item <%= "crayons-tabs--item--current" if params[:filter].to_s.casecmp("org").zero? && params[:org_id].to_i == org.id %>" href="/notifications/org/<%= org.id %>">
                <span class="crayons-tabs__label">
                  All
                </span>
              </a>
              <a class="crayons-tabs--item <%= "crayons-tabs--item--current" if params[:filter].to_s.casecmp("comments").zero? && params[:org_id].to_i == org.id %>" href="/notifications/comments/<%= org.id %>">
                <span class="crayons-tabs__label">
                  Comments
                </span>
              </a>
            </div>
          <% end %>
        <% end %>
      </section>
      <%= render "notifications_list", params: params %>
    </div>

    <%# new or less active users that don't have a page worth of notifications
        won't be shown the "load more" button %>
    <% if @notifications.size >= @initial_page_size %>
      <div class="load-more-wrapper">
        <button id="load-more-button" type="button" class="crayons-btn crayons-btn--secondary">
          Load More
        </button>
      </div>
    <% end %>

    <div class="side-bar sidebar-additional"></div>
  </div>
  <%= render "articles/fitvids" %>
<% else %>
  <%= render "devise/registrations/registration_form" %>
<% end %>
