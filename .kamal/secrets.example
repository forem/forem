# Secrets defined here are available for reference under registry/password, env/secret, builder/secrets,
# and accessories/*/env/secret in config/deploy.yml.
#
# Copy this file to `.kamal/secrets`, keep it out of git, and replace the sample
# values with secure references (environment variables, files, or secret helpers).
# DO NOT STORE RAW SECRET VALUES IN THIS FILE.

# Container registry credentials
KAMAL_REGISTRY_PASSWORD=$KAMAL_REGISTRY_PASSWORD

# Rails credentials and runtime configuration
RAILS_MASTER_KEY=$(cat config/master.key)
POSTGRES_PASSWORD=$POSTGRES_PASSWORD
DATABASE_URL=$DATABASE_URL
FOREM_OWNER_SECRET=$FOREM_OWNER_SECRET
HONEYBADGER_API_KEY=$HONEYBADGER_API_KEY
