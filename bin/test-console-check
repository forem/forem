#!/bin/bash
if [ "$RAILS_ENV" = "test" ]; then
  RAILS_ENV=production bin/rails db:create
  RAILS_ENV=production SAFETY_ASSURED=1 bin/rails db:schema:load
  bin/rails runner -e production 'puts "App booted successfully"'
else
  echo "Not in safe test environment" && exit 1
fi
